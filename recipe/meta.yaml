{% set name = "conda-package-handling" %}
{% set version = "1.1.4" %}
{% set sha256 = "8b9fe070326d9844452248f11504aaa6dc75b8eb5a900f2706df75afd1d812c4" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://github.com/conda/{{ name }}/releases/download/{{ version }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  script: "{{ PYTHON }} -m pip install . --no-deps -vv"
  # ideally this would work, but this is a conda dep, so we need to be careful.
  # noarch: python
  entry_points:
    - cph = conda_package_handling.cli:main

requirements:
  host:
    - python
    - pip
  run:
    - futures   # [py27]
    - libarchive >=3.3.3
    - python
    - python-libarchive-c
    - six
    - tqdm

test:
  source_files:
    - tests
  requires:
    - pytest
    - pytest-cov
  commands:
    - pytest tests

about:
  home: https://github.com/conda/conda-package-handling
  license: BSD-3-Clause
  license_family: BSD
  summary: Create and extract conda packages of various formats

extra:
  recipe-maintainers:
    - msarahan
